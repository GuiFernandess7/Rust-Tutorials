// Allocates the value in static memory
// Aloca o valor na memória estática
static _Y: u32 = 13;

fn main() {
    // Value below are allocated in stack memory
    // Valores abaixo são alocados na memória stack
    let x = 5;
    let z = true;
    let numbers = [1, 2, 3];

}
